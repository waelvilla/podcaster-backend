overwrite: true
schema: "./src/schemas"
documents: null
generates:
  generated/graphql-types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
